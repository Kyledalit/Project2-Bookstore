#include <iostream>
#include <string>

using namespace std;

class BookStoreManager {

private:
  
  Book[];
  int nmbrBooks;
  int Capacity[];
  static const int INIT_SIZE = 2;
  static const int RESIZE_FACTOR = 2 * INIT_SIZE;


public:

  BookstoreManager();
  BookstoreManager(const BookstoreManager&);
  ~BookstoreManager();
  
  const BookstoreManager& operator= (const BookstoreManager&);

//Mutator Methods
  
  void insert(const Book&){
  
  }

  bool remove(const Book&){
  
  }
  
  int removePublisher(const string&){
  
  }
  
//Observer Methods

  bool isEmpty();
  unsigned capacity();
  unsigned listSize();
  unsigned search(const Book&);
  string toString();
  

}
